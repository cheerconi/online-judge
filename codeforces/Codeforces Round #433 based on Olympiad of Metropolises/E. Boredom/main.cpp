#include <iostream>
using namespace std;
const int MAXN = 200000 + 10;
int pos[MAXN];
int col[MAXN], row[MAXN];

int main() {
    int n, q;
    scanf("%d%d", &n, &q);
    for (int i = 1; i <= n; i++) {
        scanf("%d", &pos[i]);
        row[i]++;
    }


    return 0;
}